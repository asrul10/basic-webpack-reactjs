# Basic Webpack ReactJS

Basic Webpack starter for ReactJS App

## Usage
```sh
npm start
```
Runs the app in development mode.

```sh
npm test
```
Runs the test.

```sh
npm run-script build
```
Builds the app for production to the dist folder.